name: Release

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]
  workflow_dispatch:
    inputs:
      release_version:
        description: "Release new version?"
        required: false
        type: boolean
        default: false
      start_date:
        description: "Contract start date:"
        required: true
        type: string
        default: "2025-10-23 19:30:00-03:00"
      seconds_in_one_week:
        description: "Seconds in one week:"
        required: true
        type: number
        default: 120
      number_of_weeks:
        description: "Number of weeks:"
        required: true
        type: string
        default: "5.2"
      network:
        description: "Network to deploy:"
        required: true
        type: choice
        options:
          - arbiSep
        default: "arbiSep"
      stake:
        description: "Staked ether:"
        required: true
        type: string
        default: "0.001"

permissions:
  id-token: write
  contents: read

jobs:
  deploy-contract-sepolia:
    name: Deploy Contracts (Sepolia)
    if: ${{ github.event_name == 'workflow_dispatch' && inputs.release_version == true }}
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Create .env for Arbitrum Sepolia deploy
        run: |
          {
            echo "arbiSep.PhysicalActivityOracle=0x9A3EdF8ce7929493C71FaBDa505C4b961f8359A9";
            echo "localhost.FitnessUnbreakableVow=0x0DCd1Bf9A1b36cE34237eEaFef220932846BCD82";
            echo "arbiSep.FitnessUnbreakableVow=0x61a5e177E732679f28A9078585c0fDB158a8F8eD";
          } > contracts/.addresses

      - name: Create event saver scheduler args
        run: |
          node -e '
            const path = require("path")
            const fs = require("fs")

            const startDate = parseInt(+new Date("${{ inputs.start_date }}") / 1000)
            const secondsInOneWeek = Number("${{ inputs.seconds_in_one_week }}")
            const numberOfWeeks = Number("${{ inputs.number_of_weeks }}")
            const network = "${{ inputs.network }}"
            const endDate = startDate + secondsInOneWeek * numberOfWeeks
            const addressesFile = path.join(__dirname, "contracts", ".addresses")

            const contractAddress = fs.readFileSync(addressesFile, "utf8")
              .split("\n")
              .find(a => a.startsWith(network + ".FitnessUnbreakableVow"))
              .split("=")[1]

            console.log("RATE=\"rate("+ parseInt(secondsInOneWeek / 60) +" minutes)\"")
            console.log("START_DATE=\"" + new Date(1000*(startDate + secondsInOneWeek + 3)).toISOString() + "\"")
            console.log("END_DATE=\"" + new Date(1000*(endDate + 10)).toISOString() + "\"")
            console.log("CONTRACT_ADDRESS=\"" + contractAddress + "\"")
          ' >> scheduler-args.txt

      - name: Upload contract addresses artifact
        uses: actions/upload-artifact@v4
        with:
          name: contract-addresses
          path: contracts/.addresses
          include-hidden-files: true

      - name: Upload scheduler args
        uses: actions/upload-artifact@v4
        with:
          name: scheduler-args
          path: scheduler-args.txt

  update-scheduler:
    name: Update Scheduler
    needs: deploy-contract-sepolia
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Download scheduler args
        uses: actions/download-artifact@v4
        with:
          name: scheduler-args
          path: scheduler-args

      - name: Configure AWS credentials (OIDC recommended)
        uses: aws-actions/configure-aws-credentials@v4
        with:
          role-to-assume: ${{ secrets.AWS_OIDC_ROLE_ARN }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Configure scheduler
        run: |
          source scheduler-args/scheduler-args.txt
          SCHEDULER="save-contract-events"
          LAMBDA_ARN="${{ secrets.SAVE_EVENTS_LAMBDA_ARN }}"
          SCHEDULER_ROLE="${{ secrets.SCHEDULER_ROLE }}"

          aws scheduler update-schedule \
            --name "$SCHEDULER" \
            --schedule-expression "$RATE" \
            --flexible-time-window "Mode=OFF" \
            --start-date "$START_DATE" \
            --end-date "$END_DATE" \
            --region us-east-1 \
            --target '{
              "Arn": "'"$LAMBDA_ARN"'",
              "RoleArn": "'"$SCHEDULER_ROLE"'",
              "Input": "{\"contractAddress\":\"$CONTRACT_ADDRESS\"}"
            }'
