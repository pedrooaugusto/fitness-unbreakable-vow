/*  [[AUTO GENERATED]]
    ---
    Check `pre:compile` task in `hardhat.config` to know more.
*/

// SPDX-License-Identifier: MIT
pragma solidity ^0.8.28;

import { P256 as OpenzP256 } from "@openzeppelin/contracts/utils/cryptography/P256.sol";

/**
 * @dev Hardhat does not implement `RIP 7212`, so we cannot use the precompile `0x100`
 * to efficiently validate signatures, instead we validate fully onchain. Not an issue
 * in development.
 */
library P256 {
    function verifyNative(bytes32 h, bytes32 r, bytes32 s, bytes32 qx, bytes32 qy) internal view returns (bool) {
        return OpenzP256.verifySolidity(h, r, s, qx, qy);
    }
}